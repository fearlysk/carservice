const routes = {
    HOME: "/",
    LOGIN: "/login",
    REGISTRATION: "/registration",
    REPAIR: "/repair",
    PROFILE: "/profile",
    USER_PROFILE: "/profile/:userId",
    ADMIN: "/admin"
}

export default routes;
